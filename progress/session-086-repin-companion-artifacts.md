# Session 086: repin companion artifacts

## Task

Drive the red consumer repin pull requests to green, root-cause the class,
and fix the class in the central repin automation.

## Baseline

- Central `main` at `effa1605b`: all three main workflows green
  (enrollment audit, Build lock CI, merge-policy audit).
- Four red repin pull requests for the authorized release v1.14.2
  (`d79e1cc`): unity-helpers #751, DxMessaging #567, IshoBoy #877,
  qora-redux #382.
- One stale open offer: DoxReloaded #801 for the older v1.14.0 release.
- unity-helpers #749 (enrollment contract fixes) already green with both
  Bugbot findings fixed and answered; merge stays the consumer decision.

## RCA: one root cause per red repin pull request

Every failure is the same class. The repin rewrite moved only `uses:` pins.
Each consumer holds a reviewed artifact that derives from the pin, so the
pin-only commit was born red:

| Repository | Failing gate | Derived artifact |
| --- | --- | --- |
| unity-helpers #751 | `portable-cleanup-classifier` parity test | The test deep-equals the whole classifier verdict object. v1.14.2 adds the reviewed `licensingCodesChecked` and `licensingCodeMatched` fields (#224), so the strict compare failed. |
| DxMessaging #567 | docs contract test, then `Script tests` and both devcontainer smoke jobs | Copyable examples in `docs/ops/ci-and-github-settings.md` and `docs/ops/ambiguous-release-migration.md` must name the pinned SHA and version comment. |
| IshoBoy #877 | `lint-build-lock-pins` pre-commit hook | `scripts/build_lock_policy.json` mirrors the central allowlists; v1.14.2 was missing. |
| qora-redux #382 | 8 static-validation tests, then 1 more | Two contract files restate the pin: `policyCommit` in `tests/ci/unity-workflow-contract.test.mjs` (8 derived expectations) and `policyCommit` in `tests/github/unity-ci.test.mjs` (1 more, found by the re-run). |

Evidence: failing-run logs for runs 34321201262 (unity-helpers), 34321140160
and 34321140103 (DxMessaging), 34321159798 (IshoBoy), 34321169391
(qora-redux). The qora failure list collapses to one constant: one line
change fixes all eight tests.

The DxMessaging and qora contracts are deliberate: a bump pull request must
carry the derived artifact. The IshoBoy hook documents the same rule. The
gap was central: the repin automation offered commits those contracts must
reject.

## Central change: reviewed `repinCompanions`

- `unity-enrollment-policy.json` gains `repinCompanions`. Each entry names a
  repository, one normalized path outside `.github/`, and one mechanical
  mode:
  - `pin-lines`: the same `uses:` pin rewrite on every line of the file
    (DxMessaging docs).
  - `pin-literal`: replaces exactly the pinned SHAs this rewrite removes,
    never any other SHA (qora-redux pin constant; the historical witness
    constant survives).
  - `policy-snapshot`: mirrors the reviewed `approved*Shas` lists exactly,
    the same content a consumer snapshot `--refresh` derives (IshoBoy).
- `tools/workflows/repin-consumer-locks.sh` validates the entries with the
  same rules as the registry parser, applies the mode-bound rewrite, stages
  the changed companions, and lists them in the pull request body. A missing
  companion file is visible in the run log and the body; it never fails
  silently.
- `internal/enrollment/unity_policy.go` registry parsing
  (`unity_registry.go`): new `UnityRepinCompanion` type with strict path,
  mode, canonical-repository, and duplicate validation.
- Live policy declares five companions: two DxMessaging docs files
  (`pin-lines`), the IshoBoy snapshot (`policy-snapshot`), and both
  qora-redux contract files (`pin-literal`).

The unity-helpers parity test is not a companion artifact: no mechanical
rewrite can decide which verdict fields a consumer's test owns. It is a test
bug (strict object compare over a verdict object the consumer does not own),
fixed on the repin branch.

## Consumer commits (pushed to the open repin branches)

- unity-helpers #751: `1d6092c` pins reviewed verdict fields per case, adds
  the 20111/400006/20113 attribution expectations, and pins the reviewed
  vocabulary string. Verified locally against a worktree at the exact pinned
  commit `d79e1cc`: 11 classifier cases and 9 gate cases pass.
- DxMessaging #567: `d0fb3b8` carries both copyable examples to
  `d79e1cc # v1.14.2`, generated by the new `rewrite-pins` mode `pin-lines`.
- IshoBoy #877: `5232eb04` appends `d79e1cc` to both snapshot allowlists,
  generated by the new `policy-snapshot` mode; byte-equivalent to a
  `--refresh` run.
- qora-redux #382: `c5e2057` moves `policyCommit` to `d79e1cc`, applied as
  the identical single-line edit the `pin-literal` mode performs. The mode
  could not generate it post hoc because the branch's workflows were already
  repinned in an earlier run, so this run had no removed pin to substitute.
  An atomic repin run regenerates the companion itself; a divergent
  missed-cycle companion fails the run closed for hand review, which is what
  this remediation was. CI then found the second constant; `02332c1` moves
  `tests/github/unity-ci.test.mjs` the same way, and both files are now
  declared companions.
- DoxReloaded #801 closed as superseded: `main` already pins v1.14.2 through
  merged #810, so the v1.14.0 offer could never merge and stayed red.

Each pushed commit has an RCA comment on its pull request.

## Verification

- Central CI on pull request #262: Build lock CI green; all workflow
  validation jobs green.
- `node --test test/*.test.js`: 879 pass, 0 fail (36 repin-script tests,
  including the adversarial-round regressions).
- `go test ./...`, `go test -race ./...`: pass, including new registry
  companion validation tests.
- `go vet`, `go mod verify`, `go mod tidy -diff` (both modules): clean.
- `golangci-lint run --timeout=5m`: 0 issues.
- `bash tools/workflows/ci.sh javascript`: clean.
- `bash tools/workflows/ci.sh shellcheck`: clean.
- `node tools/llm-harness.mjs check`: pass.
- `go run ./cmd/workflow-credential-audit .`: pass.
- Local parity proof for unity-helpers: the fixed test passes against a
  worktree at the exact pinned commit `d79e1cc`: 11 classifier cases and 9
  gate cases.

## Consumer outcomes (2026-09-09, after the companion pushes)

- unity-helpers #751: green, 22 of 22 checks pass, including
  `Contract Suites` (the parity fix proven in CI) and the
  `Unity CI Success` aggregate. Awaiting the consumer merge.
- IshoBoy #877: green; every check passes after the snapshot refresh.
- qora-redux #382: green; every check passes after both pin constants
  moved.
- DxMessaging #567: closed by the consumer at 2026-09-09T16:18Z as
  superseded by their own #568, which carries the complete seven-file pin
  cohort including the companion docs files. The queued Unity legs of #567
  then failed `require-current-pr-head` with `Pull request #567 is not
  open`: the fail-closed head guard worked as designed, refusing results
  for a closed pull request. #568 is open and mergeable.

## Adversarial review round

An independent adversarial review of the branch found five defects that the
fixes above now cover:

1. A `pin-literal` companion left stale when no workflow pin was removed
   reported a green "already pinned" row. The rewrite now fails closed when
   such a companion names no target pin anywhere; a healed companion names
   the target beside any reviewed witness, so idempotent reruns stay green.
2. The `pin-literal` substring replace could corrupt a longer hex constant
   that embedded the old pin. Replacement is now boundary-aware: standalone
   tokens only.
3. The standalone rewrite accepted policies the registry parser rejects
   (unknown fields, wrong schemaVersion or organization, unknown allowlist
   keys mirrored into snapshots). The rewrite now accepts exactly the
   reviewed policy fields, validates schemaVersion and organization, mirrors
   only the three reviewed allowlist keys, and rejects unknown entry fields.
4. A companion that is a symlink or directory made the rewrite write outside
   the checkout or fail obscurely. The rewrite refuses any companion that is
   not a regular file, verified by test.
5. Companion-only pull requests claimed "only the `@<sha>` suffix changed"
   with an empty references block. The body now states companion-only
   content plainly and lists only changed companions.

Dispositions of the remaining review notes: audit-side finding codes for
stale companions are deferred to issue #261 (repin-run visibility only, now
documented), and the PLAN.md and comment claims were corrected in place.

## Knowledge promotion

No `.llm` resource change: the authoritative companion contract already
lives in `docs/consumer-enrollment.md` and `docs/operations-runbook.md`,
which are the consumer-facing references the repository map points to. A
separate LLM resource would duplicate them and drift.

## Dispositions and follow-ups

- A repo-wide sweep for the old SHA found IshoBoy `progress/` session
  records that name it. Those are history and stay untouched: the
  `pin-literal` mode edits only declared files.
- Consumer merges remain the consumer decision; the automation still never
  merges.
- The stale-offer lifecycle (an open offer for a target the default branch
  already outruns) has no automation answer yet; tracked in the follow-up
  issue.
- DoxReloaded has no declared companion. Its next repin offer will show
  whether its gates derive from the pin; the audit-side evidence is the
  first red run, and the policy change is reviewed then.
- Consumers must not deep-equal central runtime verdicts; additive fields
  are reviewed evolution. Noted in the follow-up issue as contract guidance.
