name: Reap stale build locks

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      operation:
        description: Reap stale state or recover a confirmed quarantine
        required: true
        type: choice
        options:
          - reap
          - recover
          - recover-incident
        default: reap
      reservation-id:
        description: Exact quarantine reservation ID (recover only)
        required: false
        type: string
      resource-safe:
        description: Confirm the external Unity activation was returned (recover only)
        required: false
        type: boolean
        default: false
      incident-id:
        description: Exact global incident ID (recover-incident only)
        required: false
        type: string
      portal-cleanup-confirmed:
        description: Confirm Unity portal cleanup (recover-incident only)
        required: false
        type: boolean
        default: false

concurrency:
  group: build-lock-reaper
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  reap:
    name: Reap stale Unity lock
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Reap stale wallstop organization build lock
        uses: ./.github/actions/reap-stale-locks
        with:
          lock-name: wallstop-organization-builds
          operation: ${{ github.event.inputs.operation || 'reap' }}
          reservation-id: ${{ github.event.inputs['reservation-id'] || '' }}
          resource-safe: ${{ github.event.inputs['resource-safe'] || false }}
          incident-id: ${{ github.event.inputs['incident-id'] || '' }}
          portal-cleanup-confirmed: ${{ github.event.inputs['portal-cleanup-confirmed'] || false }}
        env:
          BUILD_LOCK_APP_ID: ${{ secrets.BUILD_LOCK_APP_ID }}
          BUILD_LOCK_APP_PRIVATE_KEY: ${{ secrets.BUILD_LOCK_APP_PRIVATE_KEY }}
          BUILD_LOCK_READER_APP_ID: ${{ secrets.BUILD_LOCK_READER_APP_ID }}
          BUILD_LOCK_READER_APP_PRIVATE_KEY: ${{ secrets.BUILD_LOCK_READER_APP_PRIVATE_KEY }}
